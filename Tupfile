include_rules

CXXFLAGS = -Wall -Werror -I.
LDFLAGS = -lboost_regex
CMDLINES = hgrep_cmdline.yaggo
MAINS = ufasta.cc hgrep.cc

: foreach $(CMDLINES)           |> !yaggo |> {cmdlines}
: foreach $(MAINS) | {cmdlines} |> !cxx   |> {mains}
: {mains}                       |> !lxxd  |> ufasta
